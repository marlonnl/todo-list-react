import styled from 'styled-components'

export const Form = styled.form`
  max-width: 547px;
  width: 100%;

  textarea {
    resize: none;
    margin: 16px 0;
  }
`

export const Opcoes = styled.div`
  margin-bottom: 16px;
  font-weight: bold;
  font-size: 14px;
  color: #666;

  p {
    margin-bottom: 6px;
  }

  label {
    margin-right: 8px;
  }

  input {
    margin-right: 2px;
  }
`
